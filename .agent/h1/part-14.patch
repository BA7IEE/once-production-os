+维护人可向指定的已激活同事发出单条人才档案的限时处理邀请。接收前只显示邀请元数据和内部同事名字，不显示原本不可见的档案内容；明确接收后，可在人才列表和详情看到该档案。
+
+`EDIT` 只允许整理基本资料，不能改变档案生命周期、范围、维护人、来源、联系方式或原始证据。`REVIEW` 提供基本档案和字段核验的操作机会，但核验仍须选择接收人本来有权读取的独立证据来源。没有独立证据时不能确认；邀请不授予来源核准权。
+
+这是“交给同事处理”，不是永久转移所有权：原维护人保持不变和原有权限，接收人不能再次转交。发起人须同时是人物与其主来源的维护人，且原有范围允许访问。管理员也不能代替一个不可访问资料的维护人发起。共享来源下的其他人物不会随交接而开放。
+
+两位参与者可以撤销/放弃；接收人可以拒收。最长 7 天，且不超过主来源有效期。不存在无限续期、隐式修改范围、自动批准敏感权限或批量代发。
+
+## 2. 权限与一致性
+
+- 原有 `policy.personFor/sourceFor` 保持原生范围语义。仅基本档案查询、允许的编辑/字段核验调用新的 `profileAccess`。
+- 联系方式、来源原文、来源历史、原件、导入、范围变更继续走原有权限，不消费交接授权。即使接收人全局有敏感权限，也不能凭交接绕过原来的资源范围。
+- 接收前固定人物和来源版本。发起后改稿，邀请失效，需要原维护人重新核对再发出。
+- 接收后人物基本字段可以持续编辑；主来源版本、保护版本、范围、原维护人或参与者资格变化，使交接失效。原维护人改变档案生命周期时递增人物保护版本。
+- 记录双方成员版本以及范围版本；成员改权/重置后即使恢复旧角色，也不复活旧交接。未来范围成员编辑接口必须递增对应 Scope.revision，不能用原地成员变更规避版本约束。
+- 所有判断在当前业务短事务内进行，使用现有全局事务锁；未删除全局锁。没有进程级授权缓存。
+- 重放先检查当前会话与对应权限，再按领域检查。人物编辑旧回执需要当前有效档案访问；交接回执只给参与者最小元数据，重放接收回执不会再次赋权。
+- 撤销/拒收可以关闭已经过期或失效但未结束的邀请；它们不需要先恢复业务读取权。
+- 截止/撤销影响下一次服务器读写，不承诺收回用户此前已经阅读、截图或下载的内容。
+
+## 3. 数据与迁移
+
+新表 `handoffs`，Prisma 模型 `RecordHandoff`。包括参与者、绑定人物/来源、版本、用途、期限、接受/关闭时间；没有资料正文快照、联系人或自由备注。
+
+追加迁移 `202609230002_record_handoffs`，不修改两个既有迁移，不改变已有资料和范围，不自动创建授权。组合外键约束同工作空间引用，CHECK 约束状态、参与者和时间。
+
+持久状态为 `PENDING / ACCEPTED / DECLINED / REVOKED`。`EXPIRED / INVALIDATED` 是实时派生显示状态，不是依赖定时任务生效的授权状态。新邀请/重复邀请准入上限与判断由现有事务锁串行化；不得无替代约束地直接移除锁。
+
+## 4. 接口（相对 /api/v1）
+
+| 方法 | 路径 | 行为 |
+|---|---|---|
+| GET | /people/{id}/handoff-recipients | 仅人物/来源共同维护人使用；分页返回有相应用途能力的活跃成员 ID 与显示名 |
+| POST | /people/{id}/handoffs | 固定版本、目的、接收人、期限与明确确认；201 最小回执，不改变资料可见性 |
+| GET | /handoffs | 分页“收到/发出”；只看本人参与的交接 |
+| GET | /handoffs/{id} | 最小当前状态，未接收/已失效不泄露原本无权读取的人物内容 |
+| POST | /handoffs/{id}/accept | 仅接收人，复核当前所有前置条件；200 事务完成回执 |
+| POST | /handoffs/{id}/decline | 仅接收人，拒收未接收邀请 |
+| POST | /handoffs/{id}/revoke | 两位参与者均可停止未关闭交接 |
+
+所有写入沿用 CSRF、Idempotency-Key、expectedRevision、审计与回执同事务。没有匿名或外部入口。目录不返回登录名、电话、完整成员权限和账号秘密。
+
+## 5. 前端
+
+人才详情新增“交给指定同事”。侧栏新增“资料交接”，可接收、拒收、撤销、查看限时档案。未知响应保留原命令及版本后原样核对，不生成另一个授权。
+
+后端返回操作能力标志。委托编辑不显示/提交生命周期字段；敏感按钮不因全局角色较高而错误显示。字段核验选择独立可读来源，并显式确认已在获准范围内核对。
+
+## 6. 本轮实际验证
+
+隔离编辑环境 Node 22.16.0：
+
+- 核心/传输回归：162/162，通过（135 个既有 + 27 个 H1；测试用 MemoryStore）。
+- 核心及测试严格类型检查通过；使用环境实际安装的 TypeScript 和 Node 类型，不是新造声明。
+- 46 条请求契约生成对照通过。
+- 前端 TSX 与新增浏览器脚本语法检查通过；不等于 React/Nest 完整构建或浏览器运行。
+- 本环境无法解析 npm/GitHub 网络域名，未在本环境安装依赖、执行 PostgreSQL 或启动真实浏览器；未改动用户电脑。
+
+新的 PostgreSQL 用例覆盖受控访问、同键并发、敏感范围不扩大、撤销后重放拒绝、组合外键/状态约束、授权/审计/回执写后回滚。
+
+新的真实浏览器脚本覆盖私有建档、UI 发出/接收、接收响应已提交但丢失后的同键核对、基本编辑、敏感/历史/同来源旁人不可见、撤销后的实际读写拒绝。
+
+上述 PostgreSQL/浏览器及完整构建交由本 PR 的 GitHub Actions 实际运行；本节不预写 PASS。最终 head 的 job/step 状态另记 PR 验收评论，不继承基线结果。CI 是隔离测试环境，不是生产部署。
+
+## 7. 开发和复验
+
+无需本地编写代码。PR 保持独立分支；若 PR #2 还未合并，使用 stacked PR，仅审 H1 差异，先合并 #2 后把本 PR retarget 到 main，不能把 H1 合进验收分支充当 #2 的内容。
+
+真实数据接管、完整恢复/删除/导出、机构品牌及私有媒体等仍是后续一期任务。本增量不宣告 DEV-03 全部结束，也不把后续 AI 延期。
+
+应用新代码到原合成开发库前，停止 API/Worker 并先备份；确认 DATABASE_URL 指向原合成库后执行 `pnpm db:deploy`。保留既有数据、密钥和两个迁移文件，不用 db push/清库解决迁移问题。没有自动部署。
+
+后续次序仍为私有媒体 → 作品/项目 → 内部候选清单，继续补齐导出/删除/备份恢复，最后接入有界 AI。
--- a/artifacts/openapi.json
+++ b/artifacts/openapi.json
@@ -1561,0 +1562,482 @@
+        "x-mode": "COMMAND"
+      }
+    },
+    "/api/v1/people/{id}/handoff-recipients": {
+      "get": {
+        "operationId": "handoff.recipients",
+        "parameters": [
+          {
+            "name": "id",
+            "in": "path",
+            "required": true,
+            "schema": {
+              "type": "string",
+              "format": "uuid"
+            }
+          },
+          {
+            "name": "page",
+            "in": "query",
+            "required": false,
+            "schema": {
+              "type": "string"
+            }
+          },
+          {
+            "name": "pageSize",
+            "in": "query",
+            "required": false,
+            "schema": {
+              "type": "string"
+            }
+          },
+          {
+            "name": "q",
+            "in": "query",
+            "required": false,
+            "schema": {
+              "type": "string"
+            }
+          },
+          {
+            "name": "purpose",
+            "in": "query",
+            "required": false,
+            "schema": {
+              "type": "string"
+            }
+          }
+        ],
+        "security": [
+          {
+            "sessionCookie": []
+          }
+        ],
+        "responses": {
+          "200": {
+            "description": "Allowlisted response DTO; see src/dto.ts"
+          },
+          "default": {
+            "description": "Sanitized error with code, message, requestId"
+          }
+        },
+        "x-permission": "records.write",
+        "x-mode": "READ"
+      }
+    },
+    "/api/v1/people/{id}/handoffs": {
+      "post": {
+        "operationId": "handoff.create",
+        "parameters": [
+          {
+            "name": "id",
+            "in": "path",
+            "required": true,
+            "schema": {
+              "type": "string",
+              "format": "uuid"
+            }
+          },
+          {
+            "name": "Origin",
+            "in": "header",
+            "required": true,
+            "schema": {
+              "type": "string"
+            }
+          },
+          {
+            "name": "X-CSRF-Token",
+            "in": "header",
+            "required": true,
+            "schema": {
+              "type": "string"
+            }
+          },
+          {
+            "name": "Idempotency-Key",
+            "in": "header",
+            "required": true,
+            "schema": {
+              "type": "string",
+              "minLength": 8,
+              "maxLength": 128
+            }
+          }
+        ],
+        "security": [
+          {
+            "sessionCookie": []
+          }
+        ],
+        "requestBody": {
+          "required": true,
+          "content": {
+            "application/json": {
+              "schema": {
+                "type": "object",
+                "additionalProperties": false,
+                "properties": {
+                  "expectedRevision": {
+                    "type": "integer",
+                    "minimum": 1,
+                    "maximum": 9007199254740991
+                  },
+                  "expectedSourceRevision": {
+                    "type": "integer",
+                    "minimum": 1,
+                    "maximum": 9007199254740991
+                  },
+                  "recipientId": {
+                    "type": "string",
+                    "minLength": 36,
+                    "maxLength": 36,
+                    "pattern": "^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$"
+                  },
+                  "purpose": {
+                    "type": "string",
+                    "enum": [
+                      "EDIT",
+                      "REVIEW"
+                    ]
+                  },
+                  "expiresAt": {
+                    "type": "string",
+                    "format": "date-time"
+                  },
+                  "acknowledgeLimitedAccess": {
+                    "type": "boolean"
+                  }
+                },
+                "required": [
+                  "expectedRevision",
+                  "expectedSourceRevision",
+                  "recipientId",
+                  "purpose",
+                  "expiresAt",
+                  "acknowledgeLimitedAccess"
+                ]
+              }
