# 当前实现状态与接力任务

版本：0.1.0-dev.1。**开发增量，不是 v0.3 一期完成，也不是 M1 试用验收。**

定义：SOURCE_IMPLEMENTED=有对应实现；CORE_MEMORY_TESTED=在合成数据+测试 Store 上执行；DB_TESTED=在隔离 PostgreSQL 上执行所列用例；BROWSER_TESTED=本地真实浏览器完成所列链路；NOT_STARTED=未开发；NOT_ACCEPTED=该工作包未达到规格验收。

2026-09-23 Review R1：追加来源历史和失败导入显式继续已应用；锁定依赖构建、既有合成库升级、新空库 13 个 PostgreSQL 断言及受限历史浏览器读取均已实跑。浏览器中的导入继续、权限拒绝与完整异常清单仍 NOT_RUN；各工作包仍未整体验收。

2026-09-23 验收切片 A1（基线 `1ba170e`）：独立 `once_test_*` 库中的真实 API、Worker、Chrome 跑通两行导入第二行失败→页面部分完成→显式继续只补第二行→完成；丢失继续响应后原请求键和内容重放；来源暂停、版本变化与编辑权限撤销后的拒绝。最小 GitHub CI 工作流已加入，远端运行结果须单独核对。该切片不包含受控资料交接或新业务模块，也不代表完整浏览器异常清单通过。

## 1. 逐工作包核对

| v0.3 工作包 | 本次实际产物 | 状态与未完成项 |
|---|---|---|
| DEV-00 独立骨架/锁定 | Node/TS 工程、Nest/React/Prisma、配置、CLI、Worker、真实锁文件、A1 最小 CI 工作流 | 锁定/完整构建 PASS；本地空库迁移 PASS；CI 结果见本轮测试报告；正式镜像 NOT_RUN，工作包 NOT_ACCEPTED |
| DEV-01 身份/范围/审计 | 会话、激活、当前授权、敏感字段、事务审计核心；前后端 | CORE_MEMORY_TESTED；本地登录/激活/协作范围 BROWSER_TESTED；完整身份隔离及异常路径 NOT_ACCEPTED |
| DEV-02 回执/后台机制 | 当前权限重放、CAS、本地导入持久任务、租约、可重试失败的显式继续 | CORE_MEMORY_TESTED；PG 同键竞争、回滚、CAS、双 Worker 租约、部分导入续跑 DB_TESTED；A1 指定续跑和拒绝路径 BROWSER_TESTED；进程崩溃及完整 JCS 向量 NOT_RUN |
| DEV-03 人才/机构/来源/字典/导入 | 人才、来源当前版及 R1 起的追加历史、字段核验、分类、有界 JSON 预览 | PARTIAL；旧记录只有迁移基线，迁移前缺失历史不能恢复；机构/品牌、草稿交接、受控合并仍缺 |
| DEV-04 私有媒体 | 无 | NOT_STARTED，不存在上传/预览假入口 |
| DEV-05 作品/项目/文本版本 | 无 | NOT_STARTED |
| DEV-06 检索/清单/工作台 | 人才基础查询、当前范围统计、最近人才 | PARTIAL；无候选清单及完整查询解释；SQL 分页/负载待改进 |
| DEV-07 导出/合并/删除 | 无 | NOT_STARTED；不得导入正式资料当作长期资产库 |
| DEV-08 AI | 无 | NOT_STARTED，仍在完整一期，不因本次切片而延期 |
| DEV-09 运维/备份/恢复 | 隔离 epoch 核心、候选 Dockerfile、Local DB Compose | 本地 PostgreSQL 16 容器与迁移 PASS；备份/恢复实现与演练 NOT_RUN，工作包 NOT_ACCEPTED |
| DEV-10 总体验收 | 核心测试、有限 HTTP 测试、静态源码检查 | PARTIAL；完整业务旅程尚不具备，不能把单测个数当作 22 组 FR 通过 |
| DEV-11 真实样本接管 | 无 | NOT_RUN |

## 2. 需求编号映射

FR-01/02/03/04：已实现快速建档、多角色、当前来源依据、字段核验、字典及受限预览的部分。

FR-11/12：只验证内部整理/内部使用、当前来源到期/暂停的部分。额外 AI/原件/导出用途尚无功能，不声称完整 UsePolicy。

FR-14：基础姓名/别名和结构化筛选及权限一致性；无自然语言检索和高级证据解释。

FR-26/27/28：身份、范围、最小审计和本地幂等/任务规则已有合成测试；多类资源和真实平台验收未完成。

FR-06/07/08/09/13/15/18/19/20/22/29/30：依照完整规格继续开发/验收，不能整体标记实现。FR-30 仅有配置/隔离的部分基础。

FR-05/10/16/17/21/23/24/25：维持 v0.3 原定 DEFERRED；本次没有借这些延期项解释内部模块缺失。

## 3. 对抗用例对应范围

AT-01/02/09/10/12/14/15/23/24 中涉及当前身份、回执、来源或导入的子情形，已有核心测试。每个 AT 包含更多领域，**没有任何“整个 AT 已通过”的隐含结论**。例如 AT-14 的上传/作品链没有实现；AT-23 的真实旧库恢复没有执行；AT-01 的完整 JCS 向量仍待补。

2026-09-23 原基线在新建 `once_test_*` 库执行真实 PostgreSQL 集成测试：6/6 通过。R1 又在另一新建空库执行 13/13，覆盖原有并发与租约、写后故障回滚、历史约束、部分导入继续和查询次数。两个测试库均保留合成记录；没有用 SQLite 或 MemoryStore 代替 PostgreSQL 约束结果。

## 4. 下一次开发必须先做的事

依赖锁定、全后端和前端 typecheck/build、新建测试库迁移/集成测试及浏览器主链路已完成。R1 验证受限历史入口；A1 补齐指定的浏览器续跑、丢失响应和三类拒绝。其余异常响应、移动宽度、键盘焦点、受限字段及恢复仍待单独验收；发现缺陷修代码和测试，不删除阻挡检查。

下一开发切片先补受控资料交接与范围调整，并处理迁移前来源证据不可恢复的边界；再开发 DEV-04 私有媒体→DEV-05 作品/项目→DEV-06 内部候选清单，同时完成 DEV-07/09 的导出、删除、备份恢复和查询下推。每块单独评审与验收。

达到内部人工闭环后完成 DEV-08 有界 AI，再做 DEV-10/11 整体验收与真实样本试点。AI 仍属于 v0.3 完整一期，不因 A1 验收切片而延期；也不在不完整的来源/删除/媒体底座上抢先上线。

## 5. 不变的交付规则

没有确认真实环境前，不写“部署成功”“生产可用”“一期完成”。后续修改需要更新本状态表、Review、实际测试报告和文件指纹。原规格保留为输入，不能把其中目标接口或表名当作当前文件自动引用。
